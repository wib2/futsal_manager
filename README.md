## ⚙️ 서버 환경 변수 설정

`futsal_server` 폴더 내부에 `.env` 파일을 생성하고, 아래 내용을 복사해서 저장해주세요.

```env
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=1234
DB_NAME=futsal_app
DB_PORT=3306
PORT=4000
```

## database 파일에 sql문 있습니다.



